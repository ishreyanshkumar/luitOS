# LUIT Lab 3 student checkpoint - v1.2.3

Read `docs/CS3106L_Lab03_Student_Handout_v1.2.pdf` before coding.

You implement exactly four TODO regions:

- K1 in `kernel/usysinfo.c`
- U1 in `user/ulib.c`
- U2 in `user/ulib.c`
- U3 in `user/ulib.c`

**All code you write for Lab 3 must stay inside these four regions.** The official grader extracts only these bodies into a clean staff copy. Do not place required helper functions, declarations, or new includes outside the markers.

Useful commands:

```sh
make lab03-check
make clean && make
make grade
make grade LAB=3
python3 tools/package_lab03.py YOUR_ROLL_NUMBER
```

There is no report and no viva. Submit only the ZIP produced by the packaging script.
